import 'package:flutter/material.dart';

class LastReservetion extends StatelessWidget {
  const LastReservetion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ultime Prenotazioni"),
    );
  }
}
